{
  "name": "products",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/products/src",
  "projectType": "library",
  "tags": [
    "type:lib"
  ],
  "implicitDependencies": [
    "shared",
    "categories",
    "!api"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["shared:build"],
      "outputs": [
        "{workspaceRoot}/dist/libs/products"
      ],
      "options": {
        "command": "npx tsc -p libs/products/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/products/jest.config.cts",
        "passWithNoTests": true
      }
    }
  }
}
