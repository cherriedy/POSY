{
  "name": "categories",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/categories/src",
  "projectType": "library",
  "tags": [],
  "implicitDependencies": ["shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/libs/categories"],
      "options": {
        "command": "npx tsc -p libs/categories/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/categories/jest.config.cts",
        "passWithNoTests": true
      }
    }
  }
}
