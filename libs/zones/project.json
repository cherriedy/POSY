{
  "name": "zones",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/zones/src",
  "projectType": "library",
  "tags": [
    "type:lib"
  ],
  "implicitDependencies": [
    "shared",
    "!api"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/libs/zones"
      ],
      "options": {
        "command": "npx tsc -p libs/zones/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/zones/jest.config.cts",
        "passWithNoTests": true
      }
    }
  }
}
