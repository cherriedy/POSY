{
  "name": "inventory",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/inventory/src",
  "projectType": "library",
  "tags": [],
  "implicitDependencies": ["shared", "ingredients"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/libs/inventory"],
      "options": {
        "command": "npx tsc -p libs/inventory/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/inventory/jest.config.cts",
        "passWithNoTests": true
      }
    }
  }
}
