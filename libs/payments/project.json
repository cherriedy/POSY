{
  "name": "payments",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/payments/src",
  "projectType": "library",
  "tags": [
    "type:lib"
  ],
  "implicitDependencies": [
    "shared",
    "orders",
    "promotions",
    "!api"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/libs/payments"
      ],
      "options": {
        "command": "npx tsc -p libs/payments/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/payments/jest.config.cts",
        "passWithNoTests": true
      }
    }
  }
}
