{
  "name": "taxes",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/taxes/src",
  "projectType": "library",
  "tags": [
    "type:lib"
  ],
  "implicitDependencies": [
    "shared",
    "!api"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/libs/taxes"
      ],
      "options": {
        "command": "npx tsc -p libs/taxes/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/taxes/jest.config.cts",
        "passWithNoTests": true
      }
    }
  }
}
